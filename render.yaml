services:
  - type: web
    name: chess-tournament-tat
    env: node
    plan: free
    branch: v0.4
    rootDir: web
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: EMAIL_PASSWORD
        sync: false
